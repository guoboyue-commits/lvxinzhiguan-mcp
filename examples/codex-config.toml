# Example only. Do not commit a real lawyer token.
# Use the MCP HTTP / streamable HTTP option in your client.

[mcp_servers.caserun]
url = "https://your-domain.example/mcp"
headers = { Authorization = "Bearer lxzg_mcp_REPLACE_ME" }
